# LPK版本升级至0.1.11

## 分类与状态

- 分类：功能优化
- 状态：已完成

## 目标

将项目版本递增一个补丁版本，并生成对应的懒猫 `.lpk` 安装包。

## 实现

1. 根项目、Web 应用和 `package.yml` 的版本统一从 `0.1.10` 升至 `0.1.11`。
2. 删除仓库根目录中 `cloud.lazycat.app.notus` 前缀的旧 `.lpk` 包后，调用现有 LPK 打包链路。

## 验证

- 确认版本元数据统一为 `0.1.11`。
- `npm run dist:lpk`
