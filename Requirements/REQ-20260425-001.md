# REQ-20260425-001

## 基本信息

- 日期：2026-04-25
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

新增 `Requirements/` 目录，记录每次需求；明确区分 bug、功能需求、功能优化；并重构 `AGENTS.md`。

## 背景

当前仓库已有 `BUG_TRACKER.md`，但缺少统一的需求台账。需求、bug、功能优化和体验优化的记录入口不够清晰，容易把优化误记成 bug。

## 目标

1. 在根目录建立统一的需求记录目录。
2. 明确需求分类和记录流转。
3. 让 `AGENTS.md` 成为更清晰、可执行的项目协作规范。

## 落地结果

1. 新增 `Requirements/README.md`，定义目录用途、分类规则和记录流程。
2. 新增 `Requirements/INDEX.md`，作为需求总台账。
3. 新增本文件，记录本次需求本身。
4. 重写 `AGENTS.md`，将需求分类、需求记录、bug 流程、文档优先级和项目约束整理为更清晰的结构。

## 记录规则确认

- bug：进入 `Requirements/INDEX.md` 与 `BUG_TRACKER.md`
- 功能需求：进入 `Requirements/INDEX.md`
- 功能优化：进入 `Requirements/INDEX.md`
- 用户体验优化：进入 `Requirements/INDEX.md`

## 验证

- 已确认根目录新增 `Requirements/`
- 已确认 `Requirements/INDEX.md` 可作为后续需求统一入口
- 已确认 `AGENTS.md` 已加入需求分类与分流规则

