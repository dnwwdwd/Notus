# REQ-20260425-002

## 基本信息

- 日期：2026-04-25
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

同步 `CLAUDE.md` 与 `AGENTS.md` 的内容，并在 `AGENTS.md` 中明确要求二者联动维护。

## 背景

当前仓库内同时存在 `AGENTS.md` 与 `CLAUDE.md`，但两者内容已经出现偏差，容易导致不同 AI 助手读取到不一致的协作规则。

## 目标

1. 将 `CLAUDE.md` 的内容调整为与 `AGENTS.md` 完全一致。
2. 在 `AGENTS.md` 中明确规定：只要 `AGENTS.md` 发生变更，必须同步更新 `CLAUDE.md`。
3. 降低文档分叉风险，确保仓库内协作规范只有一套有效口径。

## 落地结果

1. 在 `AGENTS.md` 的“文档维护约束”中新增 `CLAUDE.md` 联动维护要求。
2. 将 `CLAUDE.md` 全量同步为与 `AGENTS.md` 完全一致的内容。
3. 在 `Requirements/INDEX.md` 中登记本次需求。

## 验证

- 已确认 `AGENTS.md` 新增 `CLAUDE.md` 同步维护规则。
- 已确认 `CLAUDE.md` 内容与 `AGENTS.md` 一致。
- 已确认本次需求已记录到 `Requirements/INDEX.md`。
