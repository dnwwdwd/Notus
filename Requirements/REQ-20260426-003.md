# REQ-20260426-003

## 基本信息

- 日期：2026-04-26
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

调整仓库文档路径说明，并明确 bug 只记录在 `docs/BUG_TRACKER.md`。

## 背景

仓库中的产品文档已经集中到 `docs/` 目录，但 `AGENTS.md`、`CLAUDE.md` 和 `Requirements/` 里的部分规则仍然引用旧的根目录路径。同时，bug 的记录规则也需要明确为只写 `docs/BUG_TRACKER.md`，避免误写到 `Requirements/`。

## 落地结果

1. 更新 `AGENTS.md` 和 `CLAUDE.md`，统一将产品文档路径改为 `docs/` 下对应文件。
2. 修改需求与 bug 的分流规则：`Requirements/` 只记录非 bug 需求，bug 只记录在 `docs/BUG_TRACKER.md`。
3. 更新 `Requirements/README.md` 与需求总台账文件，移除 bug 进入 `Requirements/` 的旧口径。
4. 删除此前误写到 `Requirements/` 的 bug 记录文件，避免规则冲突。

## 验证

1. 已确认 `AGENTS.md` 与 `CLAUDE.md` 内容保持一致。
2. 已确认 `Requirements/README.md` 与需求总台账文件不再要求将 bug 写入 `Requirements/`。
3. 已确认 bug 规则统一指向 `docs/BUG_TRACKER.md`。
