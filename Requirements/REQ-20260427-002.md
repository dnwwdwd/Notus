# REQ-20260427-002

## 基本信息

- 日期：2026-04-27
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

重新打包 Notus 的 LPK 产物。

## 背景

用户要求依据仓库内现有懒猫微服打包规范与 `docs/懒猫微服通用移植教程与规范(0425).md`，重新执行 Notus 的 LPK 打包流程，产出最新安装包。

## 当前判断

1. 项目是 Next.js Web 应用，不属于纯前端静态站点。
2. 当前仓库已存在 `lzc-manifest.yml`、`package.yml`、`lzc-build.yml`、`lzc/build-package.sh` 与 `lzc/run.sh`。
3. 现有 `lzc-manifest.yml` 采用 `application + upstreams + backend_launch_command` 结构，按既有实现继续使用 v1 单容器打包方式重新出包。

## 执行目标

1. 校验关键打包文件与元数据一致性。
2. 执行 `lzc/build-package.sh` 生成最新 `lzc-dist/`。
3. 执行 `lzc-cli project build` 重新生成 `.lpk` 产物。
4. 记录验证结果与产物信息。

## 落地结果

1. 按现有 v1 单容器打包结构完成构建，`lzc/build-package.sh` 成功生成最新 `lzc-dist/`。
2. 执行 `lzc-cli project build -o cloud.lazycat.app.notus-v0.1.0.lpk`，成功产出新的 LPK 安装包。
3. 构建过程中首次在沙箱内执行 `bash lzc/build-package.sh` 时，`npm ci` 因访问 `registry.npmmirror.com` 出现 `EAI_AGAIN`；随后在沙箱外重跑打包脚本并完成构建。
4. 产物文件为根目录 `cloud.lazycat.app.notus-v0.1.0.lpk`，大小约 75 MB。
5. 2026-04-27 16:18:31 CST 基于当前最新工作区再次重新打包，`lzc/build-package.sh` 与 `lzc-cli project build` 均成功完成，产物文件名保持不变并已覆盖更新。

## 验证

1. `sh -n lzc/build-package.sh && sh -n lzc/run.sh` 通过。
2. `lzc-cli --version` 输出 `2.0.2`。
3. `bash lzc/build-package.sh` 成功，Next.js standalone 构建通过并生成 `lzc-dist/`。
4. `lzc-cli project build -o cloud.lazycat.app.notus-v0.1.0.lpk` 成功。
5. `sha256` 校验结果为 `60c80355a516360511344e8a535af83fec53dac72593d6e9c763874f2c8b3ae5`。
6. 本次重新打包后的 `sha256` 校验结果为 `ed756d6026eea6918a0dd0188db0dbeb10e940a3ad32b9698555298951ed1b16`。
