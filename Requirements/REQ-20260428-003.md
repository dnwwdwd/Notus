# REQ-20260428-003

## 基本信息

- 日期：2026-04-28
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

升级应用版本并重新打包，规避同版本安装包缓存。

## 背景

当前安装包已经多次以同一版本号重复构建，容易让懒猫安装流程、分发缓存或本地排查继续命中旧产物。用户要求在重新打包前先处理缓存问题，再生成新的 `.lpk`。

## 预期落地

1. 升级应用包版本号，避免继续覆盖同名同版本产物。
2. 保持仓库内 `package.yml`、`notus/package.json`、`notus/package-lock.json` 版本口径一致。
3. 基于新版本重新生成 `.lpk`。

## 验证

1. 已将应用版本从 `0.2.1` 升级为 `0.2.2`。
2. 已重新执行 `lzc-cli project build` 并输出新版本 `.lpk`。
