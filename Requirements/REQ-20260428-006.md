# REQ-20260428-006

## 基本信息

- 日期：2026-04-28
- 分类：功能优化
- 状态：已完成
- 来源：用户请求

## 需求标题

将应用版本升级到 0.1.2 并重新打包。

## 背景

当前仓库与安装包版本号为 `0.1.1`。用户要求将应用版本明确升级到 `0.1.2` 后重新生成 `.lpk`，避免继续沿用旧版本号产物。

## 预期落地

1. 将仓库内应用版本口径统一更新到 `0.1.2`。
2. 重新生成对应版本的 `.lpk` 安装包。
3. 打包前继续遵守“先删除同名旧包，再执行新的打包命令”的仓库规则。

## 验证

1. 已将 [package.yml](/home/burger/Documents/projects/Notus/package.yml) 更新为 `0.1.2`。
2. 已将 [package.json](/home/burger/Documents/projects/Notus/notus/package.json) 与 [package-lock.json](/home/burger/Documents/projects/Notus/notus/package-lock.json) 顶层版本更新为 `0.1.2`。
3. 已重新生成 `cloud.lazycat.app.notus-v0.1.2.lpk`。
