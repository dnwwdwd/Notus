# REQ-20260501-008 补充运行与打包命令文档

## 背景

用户希望把当前仓库的运行、桌面联调和各类打包命令明确写进 `AGENTS.md` 与 `README.md`，方便后续直接查阅，不需要再回忆脚本名称和产物位置。

## 目标

1. 在 `README.md` 写清楚安装、Web 开发、桌面开发、Web 分发、桌面安装包和 `.lpk` 打包命令。
2. 在 `AGENTS.md` 写清楚同样的命令口径，并补充 `dev:desktop` 与 `dev:desktop:all` 的区别。
3. 同步 `CLAUDE.md`，保持与 `AGENTS.md` 完全一致。
4. 明确各类构建产物的默认输出位置。

## 结果

- 已更新 `README.md` 的开发、构建和产物位置说明。
- 已更新 `AGENTS.md` 与 `CLAUDE.md` 的常用命令段落，补齐运行方式、桌面联调方式和三类打包入口。
