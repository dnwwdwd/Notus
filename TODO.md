# Notus TODO

> 更新时间：2026-04-22
> 说明：按当前已收敛的需求口径维护，优先记录仍未完成或仍是 Mock 的部分。

## 当前口径

- 文件页是主 Markdown 编辑入口。
- 文件页与知识库页的文章编辑均改为手动保存；保存成功后只把文件加入后台索引队列，不再同步等待向量化完成。
- 知识库页默认是问答页；只有选中文件时才显示左侧文章编辑器。
- 知识库页和创作页都支持手动指定参考来源。
- 创作页点击侧边栏文件时，应在当前页基于对应文章进入创作，不跳回文件页。
- 快捷键统一在设置页维护，界面内默认不直接展示提示。

## 仍待完成

- 完整 CommandPalette 仍未实现，当前还是“搜索文章”弹层，不是完整命令面板。
- 登录页仍是演示跳转，尚未接真实 Lazycat / OIDC 认证。
- 多模态图片向量仍需用真实阿里 / 豆包 / 自定义兼容接口做实测。
- sqlite-vec 的 x86_64 / aarch64 预编译与 `.lpk` 实机部署联调仍待验证。

## 文档同步原则

- 若页面行为与旧需求不一致，以当前页面实现为准。
- 更新 PDD / PRD / PROGRESS 时，要同步清理过时表述，不保留并列口径。
