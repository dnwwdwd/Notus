# Notus TODO

> 更新时间：2026-04-19
> 说明：按当前已收敛的需求口径维护，优先记录仍未完成或仍是 Mock 的部分。

## 当前口径

- 文件页是主 Markdown 编辑入口。
- 知识库页默认是问答页；只有选中文件时才显示左侧文章编辑器。
- 知识库页和创作页都支持手动指定参考来源。
- 创作页点击侧边栏文件时，应在当前页基于对应文章进入创作，不跳回文件页。
- 快捷键统一在设置页维护，界面内默认不直接展示提示。

## 仍待完成

- 知识库问答的参考来源选择目前只接入前端交互，后端检索还没有按所选文件真正过滤。
- 创作页“基于文章创作”已改为基于 remark AST 的结构化分块，但 `/api/articles/parse` 仍是 Mock 数据源，后续还需要接入真实文章解析与保存流程。
- 代码块高亮已接入编辑器，但语言列表和高亮主题还可以进一步细化。
- 设置页快捷键已可维护，更多编辑命令还没有全部开放成可配置项。
- 搜索、知识库问答、创作 Agent 仍主要依赖 Mock 接口，后端真实逻辑未接入。

## 文档同步原则

- 若页面行为与旧需求不一致，以当前页面实现为准。
- 更新 PDD / PRD / PROGRESS 时，要同步清理过时表述，不保留并列口径。
