# 创作页 Chat 全流程业务文档

> 更新时间：2026-07-10
> 状态：已归档为兼容说明。

`/canvas` 已跳转到 `/files`，不再存在画布页面、大纲入口或块级 `@bN` Mention。

Agent Loop、文件级 diff、完整文件修订、附件解析、提问卡片和历史会话继续服务于文件工作区右侧 Notus Agent。`preview_canvas_blocks` 不再注册为 Agent 工具。

当前的完整业务流程见 `docs/文件工作区Agent业务流程.md`。
