# Notus Backlog

> 更新时间：2026-07-10
> 说明：本文件只保留后续待做事项，不再承担 bug 台账职责。Bug 统一记录在 `docs/Bug台账.md`。

## 当前边界

- 当前版本保持桌面工作区的信息结构，同时支持缩小 Web/Electron 窗口后的响应式布局；懒猫包的 iOS、Android 平台声明仍不在本轮范围内。
- 当前版本已完成文件工作区与 AI Agent 主链路，后续重点转向 Agent 任务质量、真实环境回归与部署验证。

## 业务体验优化

### 文件工作区与 Agent

- [ ] 用真实 LLM 回归 `@{path}` Mention 的文件读取、未 Mention 时的按需工具调度和文件操作预览。
- [ ] 补充 AI 面板收起后重新展开时的会话状态、运行中任务和窄宽度交互回归。
- [ ] 继续优化 Agent 的多轮上下文收敛，减少长对话后的指令漂移。

## 平台与部署

- [ ] 桌面端后续事项统一维护在 `docs/桌面端路线图.md`，本文件不再重复维护桌面端细项。

- [ ] 将当前全局文章搜索弹层升级为完整 Command Palette。
- [ ] 接入懒猫 OIDC 真实登录链路，替换现有演示登录流程。
- [ ] 在真实懒猫设备上验证 `sqlite-vec` 的 aarch64 运行兼容性。
- [ ] 完成 `.lpk` 的实机安装、升级与回归验证。

## 模型与外部能力验证

- [ ] 用真实 API Key 回归 Embedding/LLM 多提供商配置，覆盖 OpenAI 兼容接口与多模态路径。
- [ ] 继续评估工作区检索和 Agent 在真实长文、长对话场景下的稳定性。
